	<!-- Start Project Design One Section -->
	<section class="project-design-one pt-70 pb-100">
		<div class="container">
			<div class="row">
				<div class="col-md-12">
					<div class="section-title">
						<h2>{{Model.ContentItem.DisplayText}}</h2>
					</div>
				</div>
                {% shape_clear_classes my_shape %}
                    {{ Model.Content.BagPart | shape_render}}
				<div class="col-lg-12 col-md-12">
					<div class="project-more-btn">
						<a href="{{Model.ContentItem.Content.OurProjectsSection.AllCaseStudiesLink.Url}}" class="btn-style-four">{{Model.ContentItem.Content.OurProjectsSection.AllCaseStudiesLink.Text}}<span>+</span></a>
					</div>
                </div>
			</div>
		</div>
	</section>
	<!-- End Project Design One Section -->

   